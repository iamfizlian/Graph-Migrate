jtet-pstmigrate
Copyright 2026 JTET

This product is licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License. A copy of the
License is included in the LICENSE file at the root of this distribution and
also available at:

    http://www.apache.org/licenses/LICENSE-2.0

------------------------------------------------------------------------
Third-party components
------------------------------------------------------------------------

This product depends on the following third-party software, which is NOT
redistributed as part of this project. Each component remains under its own
license. Users are responsible for obtaining and licensing these components
separately.

1. readpst (libpst)
   --------------------------------------------------------------------
   Source:   https://www.five-ten-sg.com/libpst/
   License:  GNU General Public License, version 2 or later (GPL-2.0+)

   `readpst` is invoked at runtime as an external process via Python's
   `subprocess` module to extract messages from `.pst` files. It is NOT
   linked, statically or dynamically, into this product. The interaction
   is limited to spawning the binary with command-line arguments and
   reading its file-system output. Per long-standing practice and the
   GPL FAQ, this argv/file-system boundary does not propagate the GPL
   to the calling program.

   This project does NOT bundle, redistribute, or modify `readpst`. End
   users obtain it independently (via `pacman`, `apt`, `dnf`, `brew`, or
   the libpst project's own releases). See the README for per-platform
   install instructions.

2. Microsoft Graph (REST API)
   --------------------------------------------------------------------
   Source:   https://learn.microsoft.com/graph/
   Terms:    Microsoft APIs Terms of Use

   This product issues HTTPS requests to the Microsoft Graph service to
   import mail items into Microsoft 365 mailboxes. Use of the Graph API
   is governed by Microsoft's API Terms of Use; this project does not
   alter those terms.

------------------------------------------------------------------------
Bundled Python dependencies
------------------------------------------------------------------------

The Python packages listed in `pyproject.toml` are installed via `pip` from
the Python Package Index. Each retains its original license; see each
project's metadata for details. As of this writing the runtime dependency
set is permissively licensed (MIT, BSD-3-Clause, or Apache-2.0).
